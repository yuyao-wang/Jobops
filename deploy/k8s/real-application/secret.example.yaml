# Example only. This file is deliberately excluded from kustomization.yaml.
# Replace every value before adding this Secret to a private overlay.
apiVersion: v1
kind: Secret
metadata:
  name: jobops-real-application-secrets
type: Opaque
stringData:
  JOBOPS_PERMIT_HMAC_HEX: replace-with-64-lowercase-hex-characters
  JOBOPS_DASHBOARD_MASTER_SECRET: replace-with-a-random-dashboard-master-secret
  JOBOPS_WORKER_ENROLLMENT_TOKEN: replace-with-a-random-one-time-enrollment-token
